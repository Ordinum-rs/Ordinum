[package]
name = "engine"
version.workspace = true
edition.workspace = true

[dependencies]
mem = { path = "../mem" }
loom = { workspace = true, optional = true }


[features]
default = ["arena_direct"]
arena_direct = []
buffered_key_writer = []

loom = ["dep:loom"]
