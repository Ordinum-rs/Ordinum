[package]
name = "engine"
version.workspace = true
edition.workspace = true

[dependencies]
mem = { path = "../mem" }

[dev-dependencies]
loom = { workspace = true }

[features]
default = ["arena_direct"]

arena_direct = []
buffered_key_writer = []
loom = []
